# AI Coding 任务通用 Prompt 模板

适用于：
- README 很长
- 任务复杂
- AI Coding 窗口有限
- 需要多轮完成
- 可能需要频繁切换新对话窗口

项目中只保留 3 个核心文件：

- `README.md`：原始题目要求，最高优先级
- `TASK_SKELETON.md`：题目骨架，记录对题目的结构化理解
- `HANDOFF.md`：当前开发进度，每次换窗口前更新

---

# 第一轮：只读题，不写代码

## Prompt

请先完整阅读 `README.md` 和当前项目目录。

这一轮暂时不要写代码，也不要修改任何业务文件。

你的任务是先把题目理解清楚，并在项目根目录创建 `TASK_SKELETON.md`，作为后续所有 AI Coding 对话统一参考的“题目骨架”。

`TASK_SKELETON.md` 必须包含：

1. 题目最终目标
   - 最终需要实现什么功能

2. 输入数据
   - 输入来源
   - 输入格式
   - 关键字段及含义

3. 输出结果
   - 输出格式
   - 字段要求
   - 排序或格式要求

4. 核心业务规则
   - 提炼 README 中所有会影响程序逻辑的规则

5. 优先级 / 调度 / 状态转换规则
   - 如果题目中存在，请单独整理清楚

6. 边界条件
   - 空数据
   - 重复数据
   - 相同优先级
   - 资源不足
   - 非法输入
   - 极值情况
   - 其他容易遗漏的特殊情况

7. 容易理解错误或遗漏的规则

8. 当前项目结构
   - 当前已有文件分别负责什么

9. 预计需要修改的文件

10. 推荐实现顺序
   - 按 P0 / P1 / P2 排优先级

11. 验收清单
   - 满足哪些条件后，才能认为题目已经完成

要求：

- 必须以 `README.md` 为最高依据
- 不要自行添加 README 中不存在的业务规则
- 如果某条规则存在歧义，请标记为【待确认】，不要擅自猜测
- 内容尽量简洁，但不能因为简洁而遗漏关键规则
- 这一轮只分析并创建 `TASK_SKELETON.md`
- 不要开始实现代码

创建完成后，聊天中只告诉我：

1. 题目的核心目标
2. 最容易出错的 3～5 个点
3. 推荐接下来首先处理什么

不要在聊天中重复整份 `TASK_SKELETON.md`。

---

# 第二轮：检查现有代码，不修改

## Prompt

现在请同时阅读：

- `README.md`
- `TASK_SKELETON.md`
- 当前项目代码

注意：

- `README.md` 是最高依据
- `TASK_SKELETON.md` 是结构化参考
- 当前代码代表实际实现状态

这一轮暂时不要修改代码。

请逐项检查当前代码与题目要求的匹配情况，并将功能分为：

- 【已完成】
- 【部分完成】
- 【未完成】
- 【可能有问题】

重点检查：

1. 是否有 README 中要求但代码完全没有实现的功能
2. 是否存在只实现了一半的逻辑
3. 是否存在边界条件遗漏
4. 是否存在输入输出格式问题
5. 是否存在排序、优先级、状态转换等逻辑问题
6. 是否存在容易导致隐藏测试失败的问题
7. 是否有不必要的大规模重构风险

最后只给出：

- 当前整体完成情况
- 最重要的未完成项
- 当前最高优先级的 1 个任务

暂时不要开始修改代码。

---

# 第三轮：只处理一个问题

## Prompt

现在只处理刚才确定的最高优先级任务。

要求：

1. 只处理这一个问题
2. 暂时不要处理其他未完成项
3. 不要大规模重构
4. 不要主动修改已经正确且测试通过的代码
5. 优先采用最小修改方案
6. 修改前先告诉我：
   - 准备修改哪个文件
   - 准备修改什么逻辑
   - 为什么这样修改
7. 然后再进行代码修改
8. 修改完成后立即运行对应测试
9. 修改结束后告诉我：
   - 修改了什么
   - 对应 README 哪条要求
   - 测试是否通过
   - 是否还有风险

如果测试通过，再告诉我下一个最优先任务。

不要一次性处理多个任务。

---

# 后续循环：一次只做一个任务

每完成一个任务后使用：

## Prompt

当前任务已经处理完成。

请重新检查：

- `README.md`
- `TASK_SKELETON.md`
- 当前代码状态

不要重复处理已经完成的功能。

请从剩余未完成项中，只选择当前优先级最高的 1 个任务。

先告诉我为什么它是当前最高优先级，然后只处理这个任务。

要求继续保持：

- 小步修改
- 最小改动
- 不大规模重构
- 不主动改已经测试通过的代码
- 修改后立即测试

---

# 如果 AI 开始重复处理同一个问题

## Prompt

停止继续重复处理刚才的问题。

请不要修改代码。

只输出：

1. 当前已经完成的功能
2. 当前仍未完成的问题
3. 当前这个问题之前已经做过哪些修改
4. 为什么目前可能还没有解决
5. 下一步唯一应该处理的任务

不要再次执行之前做过的相同修改。

如果你发现当前思路已经重复两次以上，请明确告诉我建议切换新的对话窗口。

---

# 如果测试失败

## Prompt

当前测试没有全部通过。

请不要推翻现有实现，也不要大规模重构。

请根据失败信息定位真正原因，并优先判断属于哪一种：

1. README 题意理解错误
2. 边界条件遗漏
3. 输入输出格式错误
4. 排序 / 优先级逻辑错误
5. 状态管理错误
6. 数据结构选择错误
7. 实现 bug
8. 测试本身暴露出的隐藏规则

请先分析失败原因，再给出最小修改方案。

只修改和当前失败测试直接相关的代码。

修改后重新运行测试，并告诉我：

- 根因
- 修改位置
- 修改内容
- 测试结果

---

# 如果样例通过，准备查隐藏用例

## Prompt

当前公开样例已经通过。

现在不要立即大改代码。

请根据 `README.md` 和 `TASK_SKELETON.md` 对当前实现做一次隐藏测试风险检查。

重点检查：

- 空输入
- 单条数据
- 大量数据
- 重复数据
- 相同优先级
- 相同时间
- 极值
- 资源不足
- 非法状态转换
- 重复操作
- 不存在的 ID
- 排序稳定性
- 整数溢出
- 浮点精度
- 输入输出格式
- 状态是否会污染下一次操作
- 多次调用后的结果是否正确

请只列出真正有风险的点。

对于每个风险点说明：

- 为什么可能失败
- 当前代码是否已经处理
- 是否需要修改

如果没有明确风险，不要为了“优化”而重构代码。

---

# 准备提交前：最终检查

## Prompt

准备进行一次正式提交。

请进行最终检查，但暂时不要大规模修改。

请核对：

1. `README.md` 中所有要求是否都有实现
2. `TASK_SKELETON.md` 中验收清单是否全部满足
3. 是否还有【部分完成】或【未完成】功能
4. 所有已有测试是否通过
5. 是否存在明显隐藏测试风险
6. 是否存在调试输出、多余日志、临时代码
7. 输入输出格式是否严格符合要求
8. 是否存在未使用代码或明显错误逻辑

最后给我：

- 可以提交 / 暂时不建议提交
- 如果不能提交，只告诉我最关键的剩余问题

不要为了代码更漂亮而进行无关重构。

---

# 对话窗口快满时：更新 HANDOFF.md

## Prompt

当前 AI Coding 对话窗口快满了。

请更新项目根目录中的 `HANDOFF.md`。

注意：

- 不要新建 `HANDOFF1.md`
- 不要新建其他交接文件
- 直接覆盖更新现有 `HANDOFF.md`
- 必须以当前实际代码状态为准
- 删除已经过时的信息
- 不要简单复制之前的旧内容

`HANDOFF.md` 必须包含：

1. 当前任务目标
2. 当前代码整体状态
3. 已完成内容
4. 未完成内容
5. 部分完成或存在风险的内容
6. 已修改的主要文件
7. 关键实现逻辑
8. 当前测试情况
   - 哪些测试通过
   - 哪些测试失败
9. 已知 bug / 风险点
10. 最近一次主要修改
11. 当前下一步最优先任务
12. 哪些已经正确的代码不要重复修改

要求：

- 内容简洁
- 只保留对下一个 AI 助手继续开发有价值的信息
- 不要写成长篇开发日志
- 不要重复 README 中已经非常明确的大段内容

更新完成后，只告诉我 `HANDOFF.md` 已经更新完成。

---

# 切换到新窗口：第一条 Prompt

## Prompt

这是一个已经开发到中途的 AI Coding 任务。

请先完整阅读：

1. `README.md`
2. `TASK_SKELETON.md`
3. `HANDOFF.md`
4. 当前项目代码

优先级规则：

- `README.md` = 原始需求，最高优先级
- 当前代码 = 实际实现状态
- `TASK_SKELETON.md` = 对题目的结构化理解
- `HANDOFF.md` = 上一个 AI 对话窗口的开发交接信息

如果这些内容之间出现冲突：

优先以 `README.md` 和当前实际代码为准。

这一轮先不要修改代码。

请完成：

1. 核对 `HANDOFF.md` 与当前代码是否一致
2. 核对已经完成的功能是否真的完成
3. 核对当前仍然未完成的内容
4. 检查上一个窗口是否留下明显错误
5. 选择当前最高优先级的 1 个任务

然后只告诉我：

- 当前状态是否和 `HANDOFF.md` 一致
- 当前最高优先级任务是什么
- 为什么先处理它

不要重新从头实现项目。
不要重复已经完成的功能。
不要立即大规模重构。

---

# 新窗口确认后：继续开发

## Prompt

好的，现在只处理刚才确定的最高优先级任务。

继续遵守：

- 一次只处理一个问题
- 最小修改
- 不大规模重构
- 不修改已经正确且测试通过的模块
- 修改前说明方案
- 修改后立即测试
- 测试通过后再进入下一个任务

---

# 如果发现 TASK_SKELETON.md 有错误

## Prompt

我怀疑 `TASK_SKELETON.md` 中存在对 README 的错误理解。

请重新对照：

- `README.md`
- `TASK_SKELETON.md`

只检查二者之间不一致的地方。

如果 `TASK_SKELETON.md` 确实有错误：

1. 指出错误内容
2. 给出 README 中对应的正确要求
3. 修改 `TASK_SKELETON.md`

不要因为重新检查而重写整份文件。

`README.md` 始终拥有最高优先级。

---

# 整个 AI Coding 过程的原则

始终遵守下面的流程：

README
→ TASK_SKELETON
→ 检查当前代码
→ 找最高优先级问题
→ 一次只处理一个问题
→ 测试
→ 再处理下一个问题
→ 隐藏用例检查
→ 最终提交

如果窗口快满：

当前窗口
→ 更新 HANDOFF.md
→ 新窗口读取 README + TASK_SKELETON + HANDOFF + 当前代码
→ 核对状态
→ 继续完成下一个任务

核心原则：

**README 是需求真相。**

**当前代码是实现真相。**

**TASK_SKELETON 是题目理解。**

**HANDOFF 是当前进度。**

不要让 AI 一次性重写整个项目。

不要让 AI 在测试失败后随意大规模重构。

不要在同一个错误上无限循环。

连续两次处理同一个问题仍未解决时，优先重新分析；如果 AI 已经开始重复思路，就更新 `HANDOFF.md` 并切换新窗口。
