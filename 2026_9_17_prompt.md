# 1 第一轮：只让 AI 读题，不写代码。
```
请完整阅读 @README.md，并浏览当前项目的@ 的入口文件/配置文件（如 @main.py）。

这一轮不要深入分析业务代码，不要修改代码。

请在根目录创建 TASK_SKELETON.md，包含：

1. 最终目标
2. 输入/输出
3. 核心业务规则
4. 优先级/状态转换规则
5. 边界条件和易错点
6. 项目主要文件及作用
7. 预计需要修改的文件
8. P0/P1/P2 实现顺序
9. 验收清单

要求：
- README 是最高依据
- 不要自行补充规则
- 有歧义标记【待确认】
- 简洁但不能漏关键要求
- 不要开始写代码

完成后只告诉我：
核心目标、3~5 个易错点、建议第一步。
```
# 2 第二轮：让它检查现有代码，而不是直接重写。
```
读取 TASK_SKELETON.md 和 @ 的当前相关代码文件，重点检查 @main.py。

先不要修改代码。

请告诉我：
1. 当前已经实现了哪些功能
2. 还未完成/有风险的地方
3. 哪些地方可能导致隐藏测试失败
4. 当前最高优先级的 1 个问题
5. 最小修改方案

不要重新做需求分析，不要大规模重构。
```
# 3 第三轮：再让它实现，并要求“小步修改”。
```
只处理刚才确定的最高优先级问题。

要求：
- 最小修改
- 不改其他已完成功能
- 不大规模重构
- 优先保证正确性

修改后：
1. 说明改了什么
2. 说明对应哪个需求/风险
3. 运行现有测试

测试通过后再告诉我下一个问题。
```

# 窗口第一次满
```
我要切换窗口，请在根目录下生成 HANDOFF.md。

以当前代码和测试结果为准，不要重新分析 README，不要重复 TASK_SKELETON。

请按 TASK_SKELETON.md 中的功能项记录状态：
1.【已完成/部分完成/未完成/有风险】功能状态
2. 已通过/失败的测试
3. 当前 Bug / 风险
4. 修改过的主要文件
5. 最近一次修改
6. 下一步最高优先级的 1 个任务
7. 哪些稳定代码不要再改

生成后不要继续改代码。
```
# 新窗口
```
先读 @README.md、@TASK_SKELETON.md、@HANDOFF.md 和我 @ 的当前代码相关文件。
README 是原始需求，优先级最高；
TASK_SKELETON 是已整理的题目骨架；
HANDOFF 是上一窗口进度；
当前代码是实际实现状态。

先不要改代码。

只做：
1. 核对 HANDOFF 和代码是否一致
2. 找出未完成/有风险项
3. 只选最高优先级的 1 个问题
4. 给最小修改方案

不要重新分析整个题目，不要重复已完成内容，不要重构。
```

然后它确认后，你下一句就接：
```
只处理刚才这 1 个问题。

最小修改，不改其他已完成功能，不大规模重构。
改完后运行测试，并告诉我：
1. 改了什么
2. 测试结果
3. 下一个最高优先级问题
```

# 窗口又满了
```
当前窗口快满了，请更新根目录 HANDOFF.md。

不要新建其他 handoff 文件，直接覆盖更新。

请按当前最新代码状态重新整理：
- 已完成/部分完成/未完成/有风险
- 已通过/失败测试
- 当前 Bug / 风险
- 关键修改文件
- 最近一次修改
- 下一步最高优先级任务
- 哪些稳定代码不要再改

必须以当前代码为准，删除过时信息。
```

# 测试出错
```
测试结果如下：
【粘贴结果】

不要重写，不要重构。
先找根因，再做最小修改。
重点检查题意、边界、输入输出、排序/状态、实现 bug。
改完重新测试，并告诉我根因和测试结果。

```
